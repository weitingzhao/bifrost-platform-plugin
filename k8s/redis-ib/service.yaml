apiVersion: v1
kind: Service
metadata:
  name: redis-ib
  labels:
    app.kubernetes.io/name: redis-ib
spec:
  selector:
    app.kubernetes.io/name: redis-ib
  ports:
    - name: redis
      port: 6379
      targetPort: redis
